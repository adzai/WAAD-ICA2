{
  "name": "poll app",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "build": "ls -la && cd client && ls -la && npm install && ls -la && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.17.3",
    "express-mysql-session": "^2.1.8",
    "morgan": "^1.10.0",
    "mysql": "^2.18.1",
    "rollup": "^2.75.5",
    "winston": "^3.7.2"
  }
}
